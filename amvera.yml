meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11.2"
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /data
  containerPort: "80"
  scriptName: bot_admin.py